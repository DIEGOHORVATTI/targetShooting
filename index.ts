/* import { calculateTrajectory } from './businessRules';

const startGame = async () => {
  calculateTrajectory().then((trajectory) => {
    console.log(
      `A trajetória calculada é: x = ${trajectory.x}, y = ${trajectory.y}`
    );
  });
};

console.clear();
startGame();
 */
