{
  "scripts": {
    "start:FP": "ts-node-dev --inspect --transpile-only --respawn ./FP/main.ts",
    "start:OOP": "ts-node-dev --inspect --transpile-only --respawn ./OOP/main.ts",
    "build": "tsc"
  },
  "devDependencies": {
    "@types/node": "^18.15.7",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.0.2"
  }
}
